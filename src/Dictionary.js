import { Component } from "react"
import styles from "./Dictionary.module.css"

export default class Dictionary extends Component{
    render(){
        return(
            <div className={styles.mainBox}>Hello World</div>
        )
    }
}